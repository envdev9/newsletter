<?xml version="1.0" encoding="UTF-8"?>
<!--
    Projekt Maven do OFFLINE weryfikacji Kotlin DSL configu TeamCity (settings.kts)
    bez żywego serwera. Ten sam mechanizm co w wydaniu #1:
    wtyczka teamcity-configs-maven-plugin (goal "generate").

    UWAGA: wtyczka wymaga JDK 21. W wydaniu #3 tej rubryki NIE zdołano tego
    uruchomić (patrz code/README.md) - ten pom.xml jest niezweryfikowany.
    Uruchomienie:  mvn compile   (z JDK 21 w JAVA_HOME)
-->
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <groupId>pl.prasowka.teamcity</groupId>
    <artifactId>settings-dsl-pipeline</artifactId>
    <version>1.0-SNAPSHOT</version>
    <packaging>jar</packaging>

    <properties>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <teamcity-dsl.version>2026.3-dsl6</teamcity-dsl.version>
    </properties>

    <repositories>
        <repository>
            <id>jetbrains-teamcity-repository</id>
            <url>https://download.jetbrains.com/teamcity-repository</url>
        </repository>
    </repositories>

    <pluginRepositories>
        <pluginRepository>
            <id>jetbrains-teamcity-repository</id>
            <url>https://download.jetbrains.com/teamcity-repository</url>
        </pluginRepository>
    </pluginRepositories>

    <dependencies>
        <dependency>
            <groupId>org.jetbrains.teamcity</groupId>
            <artifactId>configs-dsl-kotlin-latest</artifactId>
            <version>${teamcity-dsl.version}</version>
            <scope>provided</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.jetbrains.teamcity</groupId>
                <artifactId>teamcity-configs-maven-plugin</artifactId>
                <version>${teamcity-dsl.version}</version>
                <executions>
                    <execution>
                        <id>generate-teamcity-config</id>
                        <phase>compile</phase>
                        <goals>
                            <goal>generate</goal>
                        </goals>
                        <configuration>
                            <dstDir>${project.build.directory}/teamcity-generated</dstDir>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>
</project>
